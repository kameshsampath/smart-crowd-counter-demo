/snowflake-apps Add a Snowflake Postgres-backed session map to the completed, map-free Smart Crowd Counter v1. Inspect the current app, follow AGENTS.md, and preserve existing features/UI. Do not restore older source.

**Storage:** Keep photos in @CROWD_COUNTER_DB.CONFERENCES.SNAPS and the SMART_CROWD_COUNTER Cortex view unchanged. Postgres stores GPS metadata only in session_locations, keyed by fully qualified stage + exact generated PUT relative path, never original filename. No Snowflake location table, Cortex SQL join, analysis-result storage/cache, or replication.

**Upload/API:** Extract GPS server-side with exifr from validated upload buffers. Store nullable lat/lng; accept zero, enforce finite geographic bounds, and treat missing/malformed EXIF as no GPS without rejecting valid photos. Persist metadata after successful PUT using retry-safe writes; report metadata failures separately from upload success because the stores are not atomic. Keep GET /api/images' long-running Snowflake query; batch-fetch GPS from Postgres and merge server-side by photo key, retaining every analysis row. On Postgres failure, return analysis without pins and a visible location warning, distinct from no GPS. Existing photos without metadata work; no automatic backfill or map-triggered AI re-analysis.

**Map:** Use client-only react-leaflet, one pin per geotagged row. Pin clicks select the matching table row/detail; retain selection across refetches by photo key. Make colocated pins individually selectable. Non-GPS photos remain usable. Include loading, no-location, and error states; preserve upload/analyze/reset progress. Proxy tiles through same-origin /api/tiles for CSP; follow AGENTS.md for User-Agent, caching, and provider policy, and show attribution. Never send photos, analysis, or EXIF payloads to the tile provider.

**Reset:** Extend confirmed shared-library reset to clear staged photos and corresponding Postgres metadata, scoped to this stage/application. Handle partial failures with retry-safe cleanup; never delete another application's metadata.

**Postgres setup:** Ask whether to reuse an existing Snowflake Postgres instance or create one. For a new instance, invoke `snowflake-postgres`, verify account/region support and privileges, propose demo-appropriate sizing, and obtain explicit cost/configuration approval before creation. For reuse, request non-secret connection details and existing secret/EAI/network-rule names. Provision session_locations outside request handling with least-privilege runtime access.

**Connectivity:** Follow `snowflake-apps` connectivity guidance to reuse or create a credential secret, HOST_PORT egress rules, and EAI for the Postgres endpoint and tile provider. Separately configure required Postgres ingress rules/network policy through `snowflake-postgres`; EAI alone does not grant ingress. Confirm network changes, avoid broad allowlists, and verify connectivity from the deployed runtime, not just locally. Wire approved objects into the manifest without replacing existing settings. Use secure runtime secret mounts, server-only pooling, parameterized queries, connection/query timeouts, and certificate-verified TLS. No passwords in chat, browser, or source control.

**Deploy:** Preserve manifest settings, including SNOWFLAKE_DATABASE=CROWD_COUNTER_DB and SNOWFLAKE_SCHEMA=CONFERENCES; add approved Postgres connectivity/tile egress. Update setup/reset docs. Test valid/missing/malformed GPS, zero/out-of-range coordinates, duplicate filenames, metadata outages, partial resets, colocated pins, and pin-to-row selection. Run project tests/build; verify location-independent AI SQL and no AI reruns from map interaction. Summarize actual target account/deployment settings, obtain confirmation, deploy, and smoke-test upload, analysis, map, selection, and reset. Return the deployed URL.
